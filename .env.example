# Production / custom API: full base URL including /api if your server uses that prefix
# VITE_API_BASE_URL=https://api.yourdomain.com/api

# Leave unset in development to use Vite proxy: /api -> http://localhost:4000 (paths stripped)
